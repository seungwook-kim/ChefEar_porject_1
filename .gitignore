# 환경변수·시크릿 — 절대 커밋 금지
.env
.env.*
!.env.example
*.pem
*.key

# Python
__pycache__/
*.pyc
venv/
.venv/
env-main/
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# 대용량 음성/모델 — git 커밋 금지(용량), 별도 공유 방식 필요
data/kss/
data/synthesized/
models/
# roundtrip 테스트로 생성되는 오디오 산출물 폴더 전부(roundtrip_audio/, roundtrip_audio_epoch24/ 등
# 에포크별로 계속 늘어남) — CSV 결과(roundtrip_cer*.csv)는 대상 아님, 그건 계속 커밋함
results/tts/roundtrip_*/

# 대용량 원본 CSV(수십MB) — git 커밋 금지, load_data.py --csv 로 외부 경로 직접 참조
data/standard/*.csv
data/kadx_raw/*.csv

# Node
node_modules/
dist/
build/
.next/
.turbo/

# OS / IDE
.DS_Store
.idea/
.vscode/
.claude/

# 로그
*.log

#test 배포 절대 하지 말 것
result_test/*
tests/test-audio/*
result_test_backup_ep13_working/*
ui/assets/*
run_local.sh
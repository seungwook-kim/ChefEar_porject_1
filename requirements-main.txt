# ChefEar Integrated Environment
# 권장 실행 환경: Python 3.12
# Python 3.11.9에서도 STT/TTS 통합 및 패키지 호환성 확인 완료
# PyTorch CUDA 12.4 별도 설치 권장:
# python -m pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu124

transformers==4.57.3
accelerate==1.12.0
peft==0.20.0
bitsandbytes==0.50.0
tokenizers==0.22.2

qwen-tts==0.1.1

huggingface_hub==0.36.2
python-dotenv==1.2.3
jiwer==4.0.0
soundfile==0.14.0
librosa==0.11.0
# ui/mic_vad.py의 VAD 판정 경로가 프레임 단위로 soxr을 직접 호출한다(requirements.txt와
# 같은 이유 — librosa.resample() 호출 오버헤드가 20ms 프레임엔 너무 커서 실시간 처리가
# 안 됨, 2026-08-24 실측).
soxr==1.1.0
pandas==3.0.5
numpy==2.4.6
tqdm==4.70.0
ipykernel==7.3.0
psutil==7.2.2

# STT 학습/추론: transformers==4.57.3 / peft==0.20.0 / accelerate==1.12.0 / bitsandbytes==0.50.0 — 통합환경 검증 완료
# TTS 학습/추론: qwen-tts==0.1.1 — STT/TTS 통합환경에서 검증 완료
# TTS 모델: Qwen3-TTS 1.7B + KSS 기반 파인튜닝 모델
# STT/TTS 모델 Repo 주소는 .env의 HF_STT_MODEL_REPO / HF_TTS_MODEL_REPO로 관리

# 제외(사용 금지): groq, piper-tts
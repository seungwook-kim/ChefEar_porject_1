# 실제 값은 .env에만 적으세요 (커밋 금지). 이 파일은 필요한 키 목록만 공유하는 용도입니다.

# 예시 (조에서 실제 사용하는 키로 교체)
# GEMINI_API_KEY=
# OPENAI_API_KEY=

# src/app.py 로그인 화면(목업 계정) — 소스에 하드코딩하지 않고 여기서 채운다
# TEST_LOGIN_ID=
# TEST_LOGIN_PASSWORD=
